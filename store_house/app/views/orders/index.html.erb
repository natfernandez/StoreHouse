<h1>Lista de Pedido</h1>

<table border=1>
  <tr>
    <th>Contacto</th>
    <th>Nº Pedido</th>
    <th>Fecha</th>
    <th></th>
    <th></th>
  </tr>

  <% @orders.each do |order| %>
    <tr>
      <td><%= order.contact_id %></td>
      <td><%= order.order_number %></td>
      <td><%= order.date %></td>
      <td><%= link_to 'Edit', order %></td>
      <td><%= link_to 'Remove', order, :confirm => 'Are you sure?', :method => :delete %></td>
    </tr>
  <% end %>
</table>

<br />

<%= link_to 'New order', new_order_path %>

